---
title: "Quick Start"
description: "Get AndanDo up and running in 5 minutes"
---

Get your AndanDo tour marketplace running locally in just a few steps.

## Prerequisites

Before you begin, ensure you have:

<CardGroup cols={2}>
  <Card title=".NET SDK 10.0" icon="microsoft">
    Download from [dotnet.microsoft.com](https://dotnet.microsoft.com/)
  </Card>
  <Card title="SQL Server" icon="database">
    Local instance or connection to remote server
  </Card>
  <Card title="Git" icon="code-branch">
    For cloning the repository
  </Card>
  <Card title="Code Editor" icon="code">
    VS Code, Visual Studio, or Rider recommended
  </Card>
</CardGroup>

## Quick Setup

<Steps>
  <Step title="Clone the Repository">
    Clone the AndanDo repository to your local machine:

    ```bash
    git clone https://github.com/Designa-Studio/AndanDo.git
    cd AndanDo/AndanDo
    ```
  </Step>

  <Step title="Configure Database Connection">
    Edit `appsettings.Development.json` with your SQL Server connection:

    ```json appsettings.Development.json
    {
      "ConnectionStrings": {
        "DefaultConnection": "Server=localhost;Database=AndandoDB;Integrated Security=true;TrustServerCertificate=true;"
      }
    }
    ```

    <Note>
      Replace with your actual SQL Server credentials if not using Windows Authentication.
    </Note>
  </Step>

  <Step title="Configure JWT Settings">
    Update JWT configuration in `appsettings.Development.json`:

    ```json appsettings.Development.json
    {
      "Jwt": {
        "Issuer": "AndanDo",
        "Audience": "AndanDo",
        "SecretKey": "your-secret-key-min-32-characters-long",
        "ExpirationMinutes": 120
      }
    }
    ```

    <Warning>
      Generate a secure secret key for production. Use at least 32 characters.
    </Warning>
  </Step>

  <Step title="Restore Dependencies">
    Restore NuGet packages:

    ```bash
    dotnet restore
    ```
  </Step>

  <Step title="Build the Application">
    Build the project to ensure everything compiles:

    ```bash
    dotnet build
    ```
  </Step>

  <Step title="Run the Application">
    Start the Blazor Server application:

    ```bash
    dotnet run
    ```

    The application will be available at:
    - HTTPS: `https://localhost:7129`
    - HTTP: `http://localhost:5004`

    <Tip>
      Use HTTPS for the best experience with secure features like ProtectedLocalStorage.
    </Tip>
  </Step>
</Steps>

## Verify Your Setup

Once the application is running, verify the key features:

<AccordionGroup>
  <Accordion title="Access the Marketplace">
    Navigate to `https://localhost:7129` to see the tour marketplace homepage with featured tours.
  </Accordion>

  <Accordion title="Create a Test Account">
    1. Click **Sign Up** in the navigation
    2. Fill in the registration form with test data
    3. Complete registration and log in
  </Accordion>

  <Accordion title="Explore the Dashboard">
    After logging in, access the host dashboard to:
    - View tour statistics
    - Manage bookings
    - Create new tours
  </Accordion>
</AccordionGroup>

## Optional: Configure External Services

For full functionality, configure these optional services:

<CardGroup cols={2}>
  <Card title="SMTP Email" icon="envelope" href="/configuration/email">
    Configure email notifications for bookings
  </Card>
  <Card title="PayPal Payments" icon="credit-card" href="/configuration/paypal">
    Enable payment processing with PayPal
  </Card>
  <Card title="Google Maps" icon="map">
    Add your API key for location features
  </Card>
  <Card title="Currency Conversion" icon="dollar-sign">
    Enable multi-currency support
  </Card>
</CardGroup>

## Common Issues

<AccordionGroup>
  <Accordion title="Port already in use">
    If ports 7129 or 5004 are already in use, modify `Properties/launchSettings.json`:

    ```json
    "applicationUrl": "https://localhost:7130;http://localhost:5005"
    ```
  </Accordion>

  <Accordion title="Database connection failed">
    Verify:
    - SQL Server is running
    - Connection string is correct
    - Database `AndandoDB` exists (or will be created)
    - User has proper permissions
  </Accordion>

  <Accordion title="JWT validation errors">
    Ensure:
    - `SecretKey` is at least 32 characters
    - `Issuer` and `Audience` match in configuration
    - System clock is accurate
  </Accordion>
</AccordionGroup>

## Next Steps

Now that you have AndanDo running, explore the platform:

<CardGroup cols={3}>
  <Card title="Architecture Guide" icon="sitemap" href="/development/architecture">
    Understand the system design
  </Card>
  <Card title="Core Features" icon="stars" href="/features/payment-integration">
    Explore marketplace capabilities
  </Card>
  <Card title="API Reference" icon="code" href="/api/tour-service">
    Dive into the service APIs
  </Card>
  <Card title="Host Guide" icon="store" href="/hosts/overview">
    Learn to create and manage tours
  </Card>
  <Card title="Configuration" icon="gear" href="/configuration/appsettings">
    Customize your installation
  </Card>
  <Card title="Deployment" icon="rocket" href="/deployment/production">
    Deploy to production
  </Card>
</CardGroup>

<Note>
  **Need help?** Check the [Installation Guide](/installation) for detailed setup instructions or visit the [Configuration](/configuration/appsettings) section.
</Note>
